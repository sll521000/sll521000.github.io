import { defineConfig } from 'astro/config';

// GitHub Pages 项目站点：site 是你的 Pages 域名，base 是仓库名（路径前缀，不能漏）
export default defineConfig({
  site: 'https://sll521000.github.io',
  base: '/personal-website',
});
